@model SKBKontur.Treller.WebApplication.Blocks.TaskDetalization.Blocks.Parts.CardReviewPartBlock

@foreach (var a in @Model.ReviewToDoListsViewModel)
{
    <span>@string.Format("Список:{3}, Пунктов:{0}/{1}, Слелующий:{2}", a.CompleteCount, a.Count, a.LastIncompletedDescription, a.ListName)</span>
}
